# 튜플
한번 값을 할당하면 바꿀수 없다. 

a= [1,2,3]
a[0] = 4

값을 변화시키고 싶지 않을때 사용한다. 
b=(24, 365) -> 하루는 24시간, 1년 365일 등
변하지 않는 값을 할당하기 때문에 메모리를 효율적으로 해준다.
사용빈도는 낮다. 
개념적으로 알아두라!

a = [1,2,3, "aaa", 3.1, [1,2,3]]  리스트는 중간에 어떤 형태든 다 집어 넣을 수있다. 
파이썬에서는 형태를 지정할 필요없이 리스트에 다 집어 넣어도 관계없다. 
리스트안에 리스트를 넣는 것도 가능하다 -> 2차원 리스트

a = [[1,2,3], [4,5,6]]
3만 출력하고 싶을경우
print(a[0][2])

a = [[1,2,3],[4,5,6],[5,6,7]] -> 2차원 리스트  (괄호가 2개)

2차원의 공간에 값을 할당한것
