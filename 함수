# 함수 
코드의 재활용이 가능. 복잡한 코드도 가능. 
ctrl+c, ctrl + v 를 반복하지 않아도 된다. 

def add_test(x, y):
    print(x + y)
add_test(1,3)


def add_test(x,y):
    return x+y
print(add_test(1,3))


def add_test(x,y):
    return x+y
c= add_test(1,3)
print(c)


함수는 왜 사용하는가!

100줄의 함수를 정의한것을 하나의 문장으로 함수를 가져와 사용할 수 있다. 
