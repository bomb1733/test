
## 버블 정렬(0,1비교, 1,2비교, 2,3비교-> 쭉 끝에까지 하고서 한턴, 이것을 길이만큼 또 반복(길이의 제곱번 반복))

a = [3,5,1,2,7]
for j in range(len(a)): # 길이의 제곱번 반복하기 위해 j라는 변수번 다시 한번 실행한다
    for i in range(len(a) - 1): # 맨 마지막까지 하게되면 에러가 난다.(7이후에는 숫자가 없으므로 n-1까지)
        if a[i] > a[i+1]:
            a[i], a[i+1] = a[i+1], a[i]   # 왼쪽이 더 클경우 위치를 바꿔주는 명령문

print(a)
